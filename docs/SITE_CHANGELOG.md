# OnTimer Website Changelog

This changelog records meaningful website fixes, improvements, and maintenance outcomes. It complements Git history without duplicating implementation details. Brand strategy and positioning changes belong in BrandOS instead.

## Unreleased

### 2026-09-05

- Airport Intelligence Phase 1 deployed to production (commit `b6c7335`); verified live via the LGA calculator and a direct `security-wait` API call showing correct `airportCode: "LGA"` resolution, the normalized `intelligence` object, and backward-compatible `min/avg/max` fields.
- Decided against the TSAWaitTimes.com $49.95/month documented API. It returns the same third-party blended estimate as the free legacy endpoint already in use — paying buys terms-of-service compliance, not better data — and OnTimer's internal historical-pattern model already carries most of the recommendation quality when live evidence is weak or absent. The free legacy adapter continues unchanged at $0; see `docs/AIRPORT_INTELLIGENCE_PROJECT_PLAN.md` decision log. Outreach to PANYNJ (LGA/JFK/EWR) and other airport operators for official checkpoint data is the pursued upgrade path instead.

### 2026-09-04

- Hardened Airport Intelligence Phase 1 after route-level verification: cached evidence now has freshness recalculated at use time; the in-process cache is capped at 250 entries; diagnostics include provider latency, outcome and explicit fallback reason; and airport-specific calculators pass their authoritative IATA code to prevent broad metro-name matching from classifying LaGuardia as JFK. Verification: expanded security tests covered numeric timestamps, cached staleness, bounded eviction, diagnostics, and airport-code precedence; TypeScript validation passed; a clean local LGA browser load showed the correct LGA security label with no JFK mislabel, framework overlay or console errors; and consecutive route requests returned valid normalized fallback data with negative-cache reuse.

- Completed the planning and Phase 1 foundation for Airport Intelligence without changing the calculator layout, inputs, SEO, calendar handoff, or OnTimer CTA sequence. Replaced the fixed security-wait blend with a provider-neutral engine that separates current provider evidence, predicted wait at expected airport arrival, and the conservative security allowance used in the leave-time result; moved the internal pattern from departure hour to estimated security-arrival hour; marked PreCheck/CLEAR adjustments as inferred; added bounded response parsing, staleness handling, a five-minute airport-level evidence cache, one-minute negative cache, in-flight deduplication, provider diagnostics, and an optional provider kill switch. The current TSAWaitTimes.com legacy endpoint remains only as a continuity adapter pending commercial-license review; no airport-operated feed was scraped or integrated. Added the complete seven-phase project, cost, provider, SEO/GEO, UX, analytics, privacy, and rollout plan in `docs/AIRPORT_INTELLIGENCE_PROJECT_PLAN.md`. Verification: focused security-engine parsing, prediction, staleness, trusted-traveler, cache/deduplication, hierarchy, international, future, and failure tests passed; TypeScript validation and optimized production build passed.

- Extended the calculator-first snippet safeguards across the full indexable leave-time family: generic Airport, all airport destination pages, all cruise-terminal destination pages, the general departure-time calculator, and the wake-up calculator. Static example result cards, destination worked examples, repeated rule-of-thumb FAQ answers, and exact illustrative commute times remain visible and indexable but are excluded from Google previews so they cannot masquerade as the visitor's personalized result. Calculator-specific hero copy, metadata, structured data, local planning facts, sources, and actual computed results remain eligible; Airport Theory already protected its generated result and was left unchanged. Added a source-level regression check for these safeguards. Verification: the new calculator-snippet regression plus airport and cruise answer-intent SEO suites passed; the optimized production build generated all 223 routes; production deployment `dpl_DetV2czWsfdrz5YccL51mb8mvZDk` reached Ready and was aliased to the canonical site; representative generic Airport, Newark, PortMiami, general leave-time, and wake-up pages contained the expected narrow preview controls and no obsolete 9:20 AM text; the production audit found no new sitemap, canonical, noindex, content-funnel, fetch, or internal-link issues (one existing `/terms` redirect remains); the existing sitemap was successfully resubmitted in Google Search Console on September 4 after its prior May 30 read showed only 40 discovered pages; and Google accepted separate priority-indexing requests for the high-impression general leave-time and wake-up calculator URLs (the generic airport URL had already been submitted).

- Corrected the generic airport calculator's Google snippet candidates after Search Console showed that Google's September 3 crawl was combining the generic 2-hour/3-hour planning baseline with a static 9:20 AM example. Kept the baseline visible and indexable while excluding only that repeated rule-of-thumb paragraph from search previews, and replaced the static clock-time example with an input-dependent explanation that accurately distinguishes the calculator's route, airport-arrival, parking, and terminal calculations. Metadata, structured data, calculator logic, and routing are unchanged. Verification: source review confirmed the obsolete 9:20 AM example is absent and the narrow `data-nosnippet` control is present; the airport answer-intent SEO regression and optimized production build passed, generating all 223 routes; production deployment `dpl_DKFDhmeaGXSZJMfnoLeYoJNpGzLH` reached Ready and was aliased to the canonical site; the live HTML contained the targeted snippet control and no 9:20 AM text; the production audit found no new sitemap, canonical, noindex, content-funnel, fetch, or internal-link issues (one existing `/terms` redirect remains); and Google Search Console accepted the canonical URL into its priority crawl queue.

### 2026-09-03

- Added a narrowly scoped Travelpayouts Drive loader to satisfy site-ownership verification without enabling global automatic monetization. The third-party script loads only on the public homepage, never on calculator or medication-tool routes, and follows the existing regional cookie-consent gate. Explicit Android calculator offers and their destination/general affiliate links remain unchanged. Updated the canonical Privacy Policy to disclose the homepage verification script and the request data it may process. Verification: the optimized local and Vercel builds generated all 223 routes; production deployment `dpl_AsVFvJsuP6yBVe5NdzNzU3fQJxq7` reached Ready and was aliased to the canonical site; browser network inspection confirmed the Travelpayouts loader and configuration requests succeeded on the homepage while no Travelpayouts requests occurred on calculator or protected medication routes; the Travelpayouts dashboard removed the incomplete-setup prompt and changed Drive from locked to active after its installation check; and the production site audit found no new sitemap, canonical, noindex, content-funnel, fetch, or internal-link errors (one existing `/terms` redirect remains).

- Refocused the iPhone medication-reminder guide around OnTimer's medication schedule maker before presenting manual Calendar, Health, and Siri alternatives; elevated the scheduler on the medication-timing directory and in the primary tool navigation; and added intent-specific calculator handoffs to blog articles.
- Added a production content-funnel audit that flags indexed medication, airport/flight, lateness, meeting, and appointment pages when their main content lacks a link to the relevant OnTimer tool. Updated the permanent SEO checklist with the tool-first content rule.
- Verification: optimized production build passed; production deployment `dpl_3K4Vq3VRTGoTARetbQWgjyQzgcR6` reached Ready and was aliased to the canonical site; the live medication guide returned 200 with its new tool-first copy; the production audit crawled all 198 sitemap URLs and found no content-funnel, canonical, broken-link, `noindex`, or route-family issues (the one reported redirect is the known `/terms` static-policy handoff); the updated guide kept its primary schedule action in the first viewport with no horizontal overflow at 320, 375, 414, or 768 px.

- 2026-09-03: Expanded the Android-only calculator monetization fallback across the travel-calculator family. Android results on airport, airport-destination, cruise-terminal, general leave-time, wake-up, and airport-theory calculators now replace the iOS download action with a disclosed Welcome Pickups transfer offer; the Newark calculator retains its destination-specific transfer link while all other calculators use a separately tracked general transfer link. Non-Android visitors retain the existing OnTimer App Store actions. Affiliate blocks remain excluded from search snippets and use sponsored/nofollow link attributes. Verification: device detection, calendar-link, leave-time, airport-answer SEO, and cruise-answer SEO regressions passed; the optimized local and Vercel builds generated all 223 routes; production deployment `dpl_2frZAFjme5LnCZcXUTCHGDWAuP7Z` reached Ready and was aliased to the canonical site; the general affiliate link redirected with the expected Travelpayouts attribution; an emulated Pixel 7 Airport Theory result showed the transfer action and paid-link disclosure instead of its result-level App Store action with no browser errors; and the production site audit found no new sitemap, canonical, noindex, fetch, or internal-link errors (one existing `/terms` redirect remains).

- 2026-09-03: Corrected two calculator regressions found during Android testing. Android identification now recognizes Android browsers even when their user agent omits the `Mobile` token, so the EWR-only affiliate action reliably replaces the iOS App Store action after calendar handoff. Current Location coordinates are now sent to Google Routes as a validated latitude/longitude waypoint instead of an address string. Because airport, airport-destination, cruise-terminal, general leave-time, wake-up, and airport-theory calculators all use the shared travel-time route, this restores automatic estimates after location permission across every calculator that offers Current Location. The paid route remains server-side, same-origin guarded, rate-limited, cached, and limited to one upstream request per calculation. Verification: targeted Android-device, Routes-waypoint, API-cost guard, leave-time, and airport-answer SEO regressions passed; the optimized local and Vercel builds generated all 223 routes; production deployment `dpl_DQZW8inwmXs6a3u1cH1RqwcPtqMZ` reached Ready and was aliased to the canonical site; the formerly failing coordinate shape returned traffic-aware estimates live for representative airport, cruise-terminal, and general destinations; an emulated Pixel 7 completed a live EWR calculation; and the production site audit found no new sitemap, canonical, noindex, fetch, or internal-link errors (one existing `/terms` redirect remains). The Google Calendar link was not activated during automation to avoid staging a test event in a connected account.

- 2026-09-02: Added a narrowly scoped Android monetization path to the Newark Airport leave-time result. After the calendar handoff, Android phones receive a clearly disclosed Welcome Pickups affiliate offer instead of the iPhone App Store action, with a secondary Android-waitlist link; all other devices and calculators retain their existing actions. The integration uses a direct sponsored link rather than Travelpayouts Drive, sends no calculator inputs to the affiliate URL, and records only offer impression/click context in the existing consent-aware analytics. Updated the canonical Privacy Policy and Terms for the affiliate relationship. Verification: leave-time and airport-answer SEO regressions passed; the optimized local and Vercel production builds generated all 223 routes; deployment `dpl_EHBQukK46M3sMrgT4LiGA4cxkd8F` reached Ready; the live EWR title, canonical and structured data remained intact; the affiliate link redirected to the intended Newark car-service page with the Travelpayouts account attribution; and the live site audit found no new sitemap, canonical, noindex, fetch, or internal-link errors (one existing `/terms` redirect remains).

- 2026-09-02: Made medication-tool actions easier to find and more specific on pet, caregiver, iPhone setup, calendar setup, and ADHD timing guidance. Replaced generic schedule-generator anchors with descriptive calendar-schedule actions; the pet guide now presents a prominent pet-owner CTA and separate veterinary-practice route. Verification: medication scheduling regressions and optimized production build passed; production deployment `dpl_6NrSxvXpSwVH8wB5VpP3kgaTzrNL` reached Ready; the live SEO audit found no new sitemap, canonical, noindex, fetch, or internal-link errors (one existing `/terms` redirect remains).

- 2026-09-01: Isolated the personal, provider, caregiver, and shared medication-schedule routes from third-party analytics and scripts. Added restrictive browser security, no-referrer, and no-store response controls; centralized analytics suppression for initial loads, client navigation, and event tracking; clarified that URL-fragment schedule links are not encrypted or access-controlled; and updated the canonical static Privacy Policy and Terms with the public-tool/no-provider-relationship boundary and support-data restrictions. Verification: medication-route privacy and medication-schedule regressions passed; the optimized production build passed; browser verification confirmed all four routes loaded only same-origin Next.js scripts and no Google Analytics or other third-party scripts.

- 2026-09-01: Rebuilt the public patient medication schedule page around the immediate task of creating a calendar-ready schedule. The generator is now above the fold, with a concise prescribed-details → review → calendar-import flow, explicit clinical limits, visible FAQs and HowTo structured data, and direct caregiver/veterinary paths. Verification: medication scheduling regressions and optimized production build passed; production deployment `dpl_629qNYJKhUFjMaTzmExsk7k7XEnU` reached Ready; the live SEO audit found no new sitemap, canonical, noindex, fetch, or internal-link errors (one existing `/terms` redirect remains).

- 2026-09-01: Added privacy-safe medication-funnel measurement for patient, caregiver, veterinary, and provider flows. Events record only the audience and action for schedule generation, private-link sharing, calendar-export start/handoff, and App Store clicks; medication names, directions, dose times, dates, and link contents are excluded. Verification: medication scheduling regressions and optimized production build passed; production deployment `dpl_4wrwgnjgabQgoiVg36QJg3VAt3xj` reached Ready.

- 2026-09-01: Prepared the public caregiver and veterinary medication schedule pages for search discovery. The caregiver page is now indexable and included in the sitemap; both audience pages now display matching FAQ and HowTo structured data and link directly to related medication-schedule paths. Verification: medication scheduling regressions and optimized production build passed; production deployment `dpl_HDcrTEkAj4osxqWusbuMsMHSKf2R` reached Ready; the live site audit found no new sitemap, canonical, noindex, fetch, or internal-link errors (one existing `/terms` redirect remains).

- 2026-09-01: Made the family-caregiver medication schedule page public while keeping the provider and patient scheduling previews protected. Added a public, indexable veterinary medication-schedule handoff for veterinary practices and pet owners, including pet-owner-specific sharing and review language. Verification: medication scheduling regressions and optimized production build passed.

- 2026-08-31: Upgraded the self-serve medication schedule flow with meal-time and daily-routine starting points, editable meal/routine dose labels in calendar events, a recurring-schedule warning for as-needed directions, clearer time-zone language, browser-aware calendar handoff instructions, and a stronger post-calendar OnTimer handoff. Verified with medication schedule tests and a production build.

- 2026-08-31: Made the shared medication calendar confirmation browser-aware. Native mobile handoff now tells recipients to review and confirm the opened recurring doses; mobile download fallbacks point to the browser’s downloads; and desktop download flows explicitly direct recipients to the Downloads folder and `medication-schedule.ics`. Simplified the post-calendar OnTimer explanation to “OnTimer turns this schedule into automatic alarms.” Verification: medication regressions, optimized production build, and protected-route checks completed with this release.
- 2026-08-31: Applied the remaining provider/caregiver interview findings to the protected medication workflow. Providers now receive a patient-portal-first sharing action; timezone fields explicitly refer to the patient or recipient; daily-routine anchors support wake-up and bedtime scheduling; PRN/as-needed directions trigger a recurring-schedule safeguard; the review step previews the resulting calendar event; and desktop import help explains optional use of a separate Medication calendar. Routine labels persist through private links, recipient review, email summaries, and calendar event titles. Verification: medication schedule, share-link, and calendar export regressions plus optimized production build and protected-flow checks completed with this release.
- 2026-08-31: Clarified meal-based provider and caregiver scheduling with consistent breakfast, lunch, and dinner defaults; simplified the review heading to “Review schedule before sharing” and its adjustment guidance; and carried meal names into review cards, private recipient links, email summaries, and calendar event titles. Existing non-meal schedules and previously shared links remain compatible. Verification: medication share-link, calendar export, optimized build, and protected live flow checks completed with this release.

- Corrected the provider/caregiver medication form after role-specific review: provider guidance now assumes clinical expertise, caregiver guidance remains prescription-directed, and the tool no longer hides start date, duration, or timezone behind a disclosure. These schedule-defining fields are visible in the primary workflow, while sender attribution remains clearly optional.
- Verification: medication scheduling regressions and optimized production build passed; provider and caregiver role-specific copy, visible schedule-defining fields, meal-anchor behavior, console state, and horizontal overflow were checked in-browser at 320, 375, 414, and 768 pixels.

- Redesigned the provider and caregiver medication-scheduling flow around a three-step create, review, and share journey. Tailored the schedule-creation prompt to each audience, grouped the required scheduling fields into a clearer sequence, made dose-time editing explicit before sharing, and relocated privacy details to supporting trust copy beneath the tool.
- Replaced the passive “taken with food” checkbox with a meal-anchored scheduling choice. It now creates editable breakfast/lunch/dinner defaults for common daily frequencies while explicitly preserving the prescription label and pharmacist guidance as the source of truth.
- Verification: medication scheduling unit tests and the optimized production build passed.

- 2026-08-26: Consolidated the homepage's Organization, SoftwareApplication, WebSite, and visible FAQ structured data into one linked JSON-LD graph, giving crawlers consistent identities for OnTimer, its iPhone app, official site, App Store listing, logo, screenshots, pricing, and support contact. Removed non-evidentiary keyword/topic markup and made FAQ schema derive directly from the displayed answers so it cannot diverge from page content. Verification: the Vercel production build passed, `www.ontimer.app` serves the linked graph, and the production SEO audit reported zero issues.

- 2026-08-25: Fixed a redundant LAX headline/title ("What Time Should I Leave for LAX (LAX)?") caused by a single data entry — `src/lib/travel-locations.ts`'s LAX profile had `shortName` lazily set equal to `code` ("LAX"), unlike every other airport (e.g. LGA's `shortName` is "LaGuardia Airport"), which the shared destination-page template renders as `{shortName} ({code})`. Corrected LAX's `shortName` to "Los Angeles International Airport" (matching its existing `name` field), fixing the H1, `<title>`, meta description-adjacent copy, breadcrumb JSON-LD, and the generic airport calculator's "When to leave for LAX" related-link card everywhere `shortName` is used — all from the one data field, no template change. Audited all 61 airport/cruise entries in `travel-locations.ts` and all seed-generated entries in `international-airports.ts`; LAX was the only offender. No other route, metadata field, canonical, structured-data shape, or airport-specific page content was touched. Verification: `npm run test:airport-answer-seo`, `npm run test:leave-time`, and the optimized production build passed; browser check at 375px confirmed the longer headline wraps cleanly across 4 lines using the page's existing `min-w-0 [overflow-wrap:anywhere] max-w-4xl` H1 treatment (no CSS change needed — already present) and that the generic calculator's related-link card doesn't overflow with the longer name.

- 2026-08-25: Rewrote the opening of the "short answer"/"how it works" snippet-facing section on the generic Airport (`/airport-time-to-leave-calculator`) and Wake-Up (`/wake-up-time-calculator`) calculators so the first visible sentence leads with the calculator's differentiated inputs rather than a bare generic rule of thumb, improving the copy Google is likely to select for a featured snippet on broad "what time should I leave" queries. The generic 2-hour domestic / 3-hour international arrival baseline (Airport) and the subtract-travel-then-buffer baseline (Wake-Up) are preserved immediately after, framed explicitly as a common planning baseline rather than the primary answer; existing follow-on explanation paragraphs are unchanged except one redundant sentence removed on the Wake-Up page (which duplicated the new opening once "live traffic data" was pulled forward). No change to canonical URLs, metadata, structured data, calculator logic/inputs, analytics events, routing, internal links, or any airport/cruise destination-specific page. The general Leave-Time calculator (`/what-time-should-i-leave`) was evaluated and deliberately left unchanged — it already leads with calculator-specific framing and has no competing generic numeric rule to differentiate against. There is no generic Cruise calculator page to apply this to (only a directory page and per-terminal destination pages, which are out of scope by the same logic as airport destination pages). Verification: `npm run build`, `npm run test:leave-time`, and `npm run test:airport-answer-seo` passed; confirmed both pages still contain their respective baseline figures; browser check confirmed no duplicated sentences and no change to calculator input/output layout on either page.

- 2026-08-25: Added a 28-page medication dosage-timing SEO content cluster at `/medication-timing` (index) and `/medication-timing/[slug]`, confirmed with the product owner in advance rather than an invented list — see `src/lib/medication-timing-profiles.ts`. Each page has genuinely medication-specific content (a direct answer, 2–3 unique timing considerations, food guidance, an illustrative schedule example, missed-dose framing, and 1–2 FAQs) rather than a template with only the name swapped, following the same content-depth approach as the airport/cruise destination pages. Content is scoped strictly to general, publicly known scheduling/timing conventions (morning vs. evening, with/without food, dose spacing) — never specific dose amounts or clinical decision-making — and every page carries the same "not a medical device / not medical advice, follow your prescriber" disclaimer already used elsewhere on the site; the two controlled-substance pages (Xanax, Adderall) additionally emphasize following the exact prescribed schedule rather than general timing conventions. Each page has a self-referencing canonical, FAQPage/Article/BreadcrumbList JSON-LD, and is included in `src/app/sitemap.ts`; internal links were added from the index page, the medication scheduler's existing "Related Guides" list, and a new "Dosage Timing Guides" footer link. Also replaced the provider scheduler's plain "Medication name" field with an uncapped type-ahead/autocomplete (`src/components/MedicationAutocomplete.tsx`) backed by a new curated static dataset of ~115 common medications (`src/lib/medication-names.ts`) — architecture directly mirrors the existing Time-to-Leave airport-autocomplete (token-substring filtering, combobox with ARIA/keyboard nav, free text always preserved as a fallback) but built as new files rather than modifying the airport originals. Verification: `npm run test:medication-schedule` and the optimized production build passed, generating all 28 drug pages plus the index; browser checks confirmed self-referencing canonicals, JSON-LD types, correct 404 handling for an invalid slug, correct omission of the brand-name line for generic-only drugs, sitemap inclusion of all 29 URLs, and the new footer/related-guides links resolving correctly. `npm run audit:site` to be run against the production deploy per the SEO invariants in `CLAUDE.md`, given this change affects routes, canonicals, sitemap, and internal links.

- 2026-08-25: Replaced the provider medication scheduler's plain "Medication name" text field with a type-ahead/autocomplete (`src/components/MedicationAutocomplete.tsx`), backed by a new curated static dataset of ~115 common outpatient medications (generic + primary brand name, `src/lib/medication-names.ts`) — no external drug database or live API, matching the existing static-data pattern already used for the Time-to-Leave airport autocomplete rather than introducing a new dependency. Filtering (`src/lib/medication-autocomplete.ts`) mirrors `airport-autocomplete.ts`'s architecture exactly: uncapped, token-based substring match, sorted by prefix match then alphabetically. Free text is always preserved as a fallback — nothing is required to be selected from the list, and an explicit "No matching medication found. You can still use the name you typed." message appears rather than blocking entry. This is a separate initiative from the Phase 1/2 fixes above, per the roadmap; the static SEO landing pages half of this initiative is intentionally not started yet — the roadmap requires confirming the actual top 25–30 medication list with the product owner rather than inventing one, and per-page content needs real drug-specific facts to avoid thin/duplicate-content SEO risk. Verification: `npm run test:medication-schedule` and the optimized production build passed; browser checks confirmed matching/sorting on a representative partial query, selecting a result fills the field with the full generic (brand) label, and generating a schedule with unmatched free text still works end-to-end.

- 2026-08-25: Made the public Help hub and both help guides discoverable, indexable support pages: added compact OnTimer and App Store routes, a contextual new-user handoff after each guide, a minimal support footer, and sitemap coverage. Verification: production build and deployed sitemap/route checks pending.

- 2026-08-25: Phase 2 of the provider/caregiver medication scheduler roadmap (anchor times, food flag, calendar metadata marker; compact link encoding decided against — see below). Added routine-anchor quick-set options (Wake up 7:00 AM, Breakfast 8:00 AM, Lunch 12:00 PM, Dinner 6:00 PM, Bedtime 9:00 PM) as a pill row inside the existing dose-time wheel-picker modal — tapping one sets the wheel to that preset time, which the user can still fine-tune before confirming; this is a UI convenience only, no new field was added to the schedule data model, and it's available everywhere `DoseTimeField` is used (setup screen, provider review screen, recipient review screen). Added a single "Taken with food" checkbox on the setup screen, captured per-medication (not per-dose), which appends "— Take with food" to the generated `.ics` event description, shows as a plain line next to Instructions on both the provider review and recipient pages, and adds a "Take with food: Yes" line to the email draft summary when set. Added a consistent `LOCATION:OnTimer Medication Schedule` marker to every generated medication calendar event, distinguishing it from other OnTimer-generated events (e.g. Time-to-Leave alarms) for a future in-app alarm-filtering setting to key off of; metadata only, no filtering UI built yet. Verification: `npm run test:medication-schedule` and the optimized production build passed, including new coverage for the food flag appending correctly with and without instructions, the LOCATION marker, and taken-with-food round-tripping through the share link (and decoding as `undefined`, not `false`, when omitted from older links); browser checks confirmed the anchor pills set the correct time and the recipient page's downloaded `.ics` blob contained both the `LOCATION` marker and the food line in `DESCRIPTION`.
  - **Decided against, not implemented**: the roadmap's compact-link-encoding item (denser encoding to shorten the shareable/calendar link). This repo's own 2026-08-15 entries above document that a prior attempt at a denser Base64 link encoding was reverted after live iOS Mail testing showed it got truncated when auto-detected as a link in a plain-text email body — the verbose percent-encoded JSON format (asserted by a test as the "iOS Mail-compatible" format) is what's currently proven reliable. Flagged this history and confirmed with the product owner (2026-08-25): keep the current format; do not revisit without real-device iOS Mail verification of any replacement.
- 2026-08-24: Fixed two layout regressions on the provider medication scheduler's setup form (`/provider-medication-schedule`), caused in part by the prior "Ongoing" duration option pushing the "How long is this schedule?" row to six items and wrapping to a second line. Converted that control from a wrapping button row to a single-line `<select>` dropdown (matching the existing time-zone select already in this form) so it can never wrap regardless of viewport width or number of options; all six options (7/10/14/30/Ongoing/Custom) and the Custom day-count input are unchanged. Made "Generate Schedule" reachable without scrolling the full form: it now pins to the bottom of the form panel via `position: sticky` once the user has scrolled past the "Create the schedule" heading, so it doesn't paint over the "How often per day?" pills (or any field) at initial page load — a real overlap bug caught during testing, since a sticky element engaged from the very top of a tall form can visually cover earlier fields as they scroll past it. Field order, values, and behavior are unchanged; this is layout/positioning only. Verification: `npm run test:medication-schedule` and the optimized production build passed; direct DOM measurement in-browser (desktop 1280px and mobile 375px) confirmed the duration select never wraps, the button is reachable within ~370px of scroll on both breakpoints, it is not sticky (and doesn't overlap the Frequency pills) at initial load, and once engaged it has zero pixel overlap with the Duration field or any other field.
- 2026-08-24: Provider/caregiver medication schedule tool improvements. Added an "Ongoing" duration option alongside 7/10/14/30-day and Custom presets; the exported `.ics` recurrence omits `COUNT` entirely for that option (`RRULE:FREQ=DAILY`), which RFC 5545 defines as a valid daily recurrence with no fixed end; this was verified against spec and by inspecting the generated `.ics` output, not by live-importing into Apple Calendar, Google Calendar, and Outlook. The dose-time wheel's minute column now snaps to 5-minute increments (`:00`–`:55`) instead of every minute. Added a plain reassurance line ("You'll be able to fine-tune each dose time on the next screen.") beneath the setup screen's First dose time field. On the review/send screen, "Copy link" is now the primary action, relabeled "Copy patient portal link" for the provider variant (caregiver variant keeps "Copy private link"), with Email schedule and Text or share another way demoted to secondary underlined actions below it; no share/email/copy behavior changed, only order, emphasis, and provider-facing label. Verification: `npm run test:medication-schedule` and the optimized production build passed; browser checks confirmed the Ongoing option, 5-minute minute wheel, reassurance copy, reordered/relabeled send actions (desktop and 375px mobile, no label overflow), and a captured `.ics` blob from the recipient page showing `RRULE:FREQ=DAILY` with no `COUNT`.
- 2026-08-24: Extended the approved calculator UX pattern across Airport (generic and destination pages), Cruise, general Leave-Time, Wake-Up, and Airport Theory. Every calculator now uses one clearly labeled date picker with the full selected date, while result-producing calculators show a compact context-specific calendar preview immediately above the calendar action, preserve full destination/terminal locations in Google Calendar and `.ics` events, and use a time-independent alarm handoff. Calendar descriptions now add a restrained plain App Store link beneath “Calculated by OnTimer.” Medication tools and all existing SEO/GEO-facing copy are unchanged. Verification: calendar-link, date-label, Airport SEO, and Cruise SEO regressions passed; the optimized build generated all 193 routes; representative browser checks confirmed the new labels and removal of the old Today/Future toggle on six calculator routes.
- 2026-08-24: Replaced the generic airport calculator’s boxed, multi-row calendar-event preview with one compact sentence immediately above the calendar action, preserving the event title and leave time while removing duplicated location and label rows. Other calculator UI, medication code, downstream content, metadata, schema, and SEO/GEO copy are unchanged. Verification: calendar-link and leave-time regressions passed; the optimized build generated all 193 routes; and a complete local LAX result at 1280 × 720 showed the calendar button fully visible from y=350–398 with 322 pixels remaining below it, no former preview card, no framework overlay, and no horizontal overflow.
- 2026-08-24: Simplified the airport calculator date selection to one native date-picker target. Today now appears once as a complete label such as “Today, Monday, August 24”; future selections remain spelled out in the same field, while live-versus-expected traffic behavior continues to derive automatically from the selected date. No medication code, surrounding calculator UI, metadata, schema, or SEO/GEO copy changed. Verification: the date-label and leave-time regressions passed; the optimized local and Vercel builds generated all 193 routes; browser checks on the generic and Newark calculators confirmed the single date control, correct H1, complete page content, no framework overlay, and no horizontal overflow at the available 1280px viewport; production deployment `dpl_2FuX7oGoZ8FLZDPKWHQCuC6igspw` reached Ready, the canonical page served the corrected control, and the production error scan found no errors.
- 2026-08-24: Completed a two-phase, generic-airport-only usability rollout. Phase 1 clarifies “Flight departs at,” gives Today a persistent date indicator, and replaces the time-dependent post-calendar headline with “Don’t be late. Turn this into an alarm.” Phase 2 adds static airport type-ahead with raw-text fallback, previews the calendar event before handoff, and writes the full airport name, IATA code and city to Google Calendar and `.ics` locations. Airport-specific pages, point-to-point inputs, metadata, schema and all SEO/GEO copy remain unchanged; the generic calculator already precedes its complete long-form guidance, and the proposed compact raw-JSON link change is not applicable because this page has no such share link. Verification: airport-autocomplete, calendar-link and leave-time regressions passed; the optimized production build passed; and the local generic calculator completed a static LAX selection, manual-travel calculation, calendar-preview/payload and post-calendar CTA check with no framework error overlay or horizontal overflow at the available desktop viewport.
- 2026-08-24: Corrected calculator automatic-alarm CTA measurement. CTA views are now recorded when the actual CTA becomes visible, rather than only after a visitor has already opened a calendar handoff; clicks and App Store outbound events remain distinct. Verification: production build and calculator tracking regression check pending with this release.

- 2026-08-20: Made the optional calculation explanation more discoverable in airport and cruise Time-to-Leave results. “How we calculated your leave time” is now a restrained, full-width 44px disclosure with a clear chevron and accessible focus state, so it reads as expandable without competing with the calendar or OnTimer actions. Verification: production build passed; deployment `dpl_71cCHeuBZ2ztTARQuECvhPxsReJP` reached Ready and is aliased to the canonical site.
- 2026-08-20: Simplified the post-Google-Calendar handoff across Time-to-Leave calculators. After opening Google Calendar, the calculator now shows only a compact “Add to another calendar” recovery row with “Re-open Google Calendar” and “Other calendars”; it no longer claims the event was saved or tells people to finish saving it. Verification: shared calendar-link regression and production build passed; production deployment and canonical route check completed at release.
- 2026-08-20: Extended the approved EWR result hierarchy across the Time-to-Leave calculator family: generic and destination airport calculators, cruise-terminal, general leave-time, and wake-up. Each result now keeps a calculator-specific, state-derived trust summary and visible adjustment path near the answer, displays only the calendar action before handoff, and advances to a time-specific OnTimer alarm offer with a compact truthful calendar-open status afterward. Existing destination SEO/GEO content, metadata, schema, FAQs, and internal links are unchanged. Verification: leave-time, calendar-link, airport answer-intent, and cruise answer-intent regressions passed; local and Vercel production builds passed; production deployment `dpl_4ervt8ish4b4RhdNwmPiaWDwTfyN` reached Ready; representative airport, EWR, cruise, general leave-time, and wake-up routes returned 200; and the production site audit passed all 164 sitemap URLs and 168 internal URLs with zero issues.
- 2026-08-20: Added an EWR-only mobile result-flow experiment to the Newark Airport calculator. The leave-time answer now includes a compact, state-derived calculation summary and an immediately visible “Adjust assumptions” control before the calendar action. The result shows only one primary CTA at a time; after the calendar handoff it advances to a time-specific OnTimer alarm offer and reduces the calendar-open message to a truthful compact status. Other airport calculators and the page’s metadata, schema, headings, FAQ, internal links, and crawlable guidance are unchanged. Verification: leave-time, calendar-link, and airport-answer SEO regressions passed; local and Vercel production builds passed; final production deployment `dpl_8JVnssnUkZq9bk2XkLciCZho5DjF` reached Ready; the canonical EWR page returned 200 with its original title, canonical, and structured data; and the production site audit passed all 164 sitemap URLs and 168 internal URLs with zero issues.
- 2026-08-20: Reworked the airport calculator around the task flow: concise input-accurate hero copy; a single “What we use” summary with “Adjust assumptions”; and a result state that places the calendar action immediately after the leave time. Calculation details are now optional and include a direct way to revise trip details or assumptions. After a calendar handoff, the OnTimer offer moves before the calendar-open status so the app download action stays in the same visual area. Added the durable calculator conversion contract to `CLAUDE.md`. Verification: airport planner regressions, production build, and local mobile page-load check passed.
- 2026-08-20: Repaired GA4 production initialization by bootstrapping the analytics command queue before Google’s external tag loads, so initial page views and immediate calculator interactions are retained. The EU/EEA/UK/Swiss consent gate and private medication-route exclusion remain intact. Verification: production build and live airport-calculator tag bootstrap check passed.
- 2026-08-20: Removed the redundant “Your next step” eyebrow from every calculator action handoff, including Airport, Cruise, General Leave Time, Wake-Up, personal medication, and provider/caregiver medication flows. The Time-to-Leave calculators use specific calendar actions (“Put this leave time on your calendar.” and, for Wake-Up, “Put your arrival appointment on your calendar.”) and a benefit-led OnTimer offer (“Get an alarm when it’s time to leave.”). Verification: TypeScript production build passed.
- 2026-08-19: Simplified the shared medication recipient’s mobile action hierarchy by removing the redundant pre-calendar “Your next step” eyebrow and leading directly with “Add this schedule to your calendar.” After calendar handoff, the OnTimer card moves first, receives focus, and now leads with the concrete benefit “Get an alarm for every dose,” while the completed calendar action remains below as status and confirmation. The shared recipient component applies this behavior to both provider and caregiver links. Verification: medication regressions, optimized production build, and responsive recipient checks at 320, 375, 414, and 768 px passed.
- 2026-08-19: Reworked provider and caregiver medication-schedule email drafts into a consistent action-first plain-text format with a reliable subject, complete private link, human-readable schedule summary, and a concise organizational-use limitation. Provider drafts identify the practice; caregiver drafts use warmer nonclinical language; neither subject exposes medication details. Verification: medication share-link regression coverage and optimized production build passed.
- 2026-08-18: Added a public, mobile-first Help hub with linked troubleshooting and getting-started guides for the iOS in-app browser. The pages preserve the site’s lightweight legal-document treatment, support automatic light and dark appearance, and omit analytics and the marketing chrome for a faster support experience. Verification: production build passed; all three Help routes are statically generated.
- 2026-08-18: Added direct Help hub, troubleshooting, and getting-started links to the site footer so visitors can reach support from any public page.

### 2026-08-17

- Improved search-snippet candidates across the generic airport, airport-specific, cruise-terminal, and general Time-to-Leave calculator families without changing URLs, titles, H1s, canonicals, indexation, internal links, or structured-data types. Destination templates now lead with concise calculator-specific value and inputs; only the retained rule-of-thumb FAQ answer is excluded from snippets on airport and cruise destination pages and the generic airport calculator.
- Verification: airport and cruise snippet regression tests and the optimized production build passed; representative generated metadata and page models were checked for every calculator family, and existing informational guidance remained visible and indexable.

- Corrected the stale `/tools` URL emitted by calculator breadcrumb structured data: breadcrumb items now point directly to the canonical `/time-calculators` directory, while `/tools` permanently redirects there for previously discovered URLs. Confirmed the separately reported Seattle cruise-terminal URL now permanently redirects from the former airport namespace to its live canonical cruise page.

### 2026-08-16

- Added a separately protected caregiver medication-schedule flow for family members helping an older parent or loved one. It reuses the private client-only link, recipient review, calendar handoff, dose-time wheel, and early/late safeguards while replacing clinical provider language with warmer caregiver guidance and caregiver-specific email copy. Verification: caregiver share-role encoding and email-copy regressions passed; protected-route redirects, mobile layout, recipient handoff, and production build were verified.

### 2026-08-15

- Reworked medication time entry across the personal generator, provider handoff, and shared recipient review: a purpose-built three-column hour/minute/AM-PM wheel now mirrors the iOS alarm-setting pattern and updates schedule state immediately without requiring blur. Dose fields from 11 PM–4:59 AM and 5–6:59 AM receive yellow late/early emphasis with patient and provider-specific confirmation copy; the prior generic even-spacing warning was removed. Improved the personal medication page’s action hierarchy and trust copy while preserving its visible SEO/GEO terminology, added optional instructions to calendar events, clarified the one-file calendar workflow, reduced redundant App Store interruptions, and aligned the post-calendar state with the recipient flow. Verification: medication schedule and calendar export regressions passed, including midnight/noon/11 PM conversion and exported instruction coverage; browser checks at 320, 375, and 414 pixels found no overflow or framework errors; and immediate calendar export/private-link tests preserved a just-edited 11 PM dose without an intervening blur.
- Removed the temporarily password-protected provider medication route from the public sitemap, marked it noindex until its public launch, and pointed medication-tool privacy links directly at the canonical policy document to eliminate an unnecessary internal redirect found by the post-deployment site audit.
- Changed the recipient’s post-calendar mobile hierarchy: after returning from calendar handoff, the OnTimer alarm card moves above the completed calendar card and receives focus, while the former “Your next step” label changes to “Added to calendar.”
- Restored medication link generation to the original percent-encoded JSON hash after live iOS Mail testing confirmed that the later compact Base64 representation was truncated by Mail. Decoding remains backward-compatible with compact links already shared, but all newly generated emails use the previously working long format.
- Rolled back the native-share experiment after device testing showed that target apps could omit the subject and produce an unpolished email, while desktop share pickers did not consistently offer Mail. Restored the preceding dedicated email-draft and secondary share actions pending a deliberate decision about the zero-server-storage constraint versus reliable HTML email delivery.
- Fixed iOS Mail truncating plain-text medication links first at the compact encoding marker and then at the hash parameter’s equals sign. New links now use one uninterrupted URL-safe hash (`#v1_…`) with no key/value punctuation for Mail to split; the recipient decoder continues to accept both earlier formats so previously generated links remain valid when copied intact.
- Tightened the medication handoff’s mobile action sequence from field-tested screenshots: the provider form begins higher in the first viewport, generated schedules put email/share actions before detailed time editing on mobile, and recipient links put the medication plus calendar action before secondary schedule details. After calendar handoff, focus advances to the OnTimer alarm step. Email is now a distinct action with a reliable subject and framed body, text/native sharing remains available separately, and newly generated private links use shorter URL-safe encoding while existing links remain compatible.
- Added a temporary, production-only shared-password gate around the provider and recipient medication schedule routes so the workflow can be privately tested with clinicians and other invited reviewers without publishing it broadly. Replaced the initial browser-native authentication prompt after it interfered with form input in a reviewer’s browser; the OnTimer access screen now sets an HTTP-only access cookie and carries the client-only schedule hash through locally without sending schedule data to the server.
- Strengthened the private provider-to-patient medication handoff: optional practice names now remain inside the client-only URL hash and frame the recipient page and share drafts; email/message copy explains the link before presenting it; changed recipient dose times trigger a persistent safety warning that clears on reversion; overnight dose explanations appear in both provider and recipient review; and recipient times now show the schedule timezone used by the calendar export.
- Made calendar export browser-aware without introducing server storage: Safari receives a non-forced `text/calendar` blob navigation so the OS can handle it natively, while Chrome, Edge, and Firefox retain a file download followed by compact inline import help. Replaced the interrupting desktop dialog with the existing inline confirmation/disclosure pattern and added static private-link Open Graph metadata.
- Verification: medication schedule, calendar export, provider calendar, and private share-link regression tests passed; the optimized production build passed; the complete provider-to-recipient hash flow was exercised locally with practice attribution, timezone labeling, overnight guidance, hash removal, and edit-warning behavior; the Chromium fallback rendered its inline confirmation/help without a modal; and the protected Vercel preview served the intended private-link Open Graph title, description, and icon. Safari/Firefox OS handoff and Windows Chrome/Edge remain pending real-device verification.
- Updated the live and source-copy Privacy Policy and Terms of Service to cover website calculators, travel-location processing, browser-local medication fields, aggregate medication-tool analytics, private URL-fragment sharing, shared-route analytics exclusion, planning estimates, and medical limitations. Removed language that treated the Privacy Policy itself as blanket consent.
- Added concise, contextual Privacy Policy and Terms links to the medication generator, provider schedule workflow, and shared patient schedule without adding blanket agreement language beneath ordinary tool buttons.
- Verification: medication schedule, calendar export, provider schedule, and private share-link regression tests passed; the optimized production build passed its lint and type checks and generated all 187 static pages.

### 2026-08-14

- Connected the production website to a dedicated GA4 web stream and replaced the fragile inline analytics initializer with a consent-aware command queue, preventing page views and early calculator interactions from being silently discarded before Google’s library finishes loading. Linked the canonical Search Console property to that stream and registered the calculator, intent, airport, calendar-provider and CTA event dimensions needed for comparable 7- and 28-day airport-answer reporting.
- Verification: production build passed; the live bundle contained the dedicated web-stream ID and consent-aware command queue, Google accepted a synthetic validation event, and the post-deployment audit passed all 165 URLs with zero issues.

### 2026-08-13

- Extended the airport answer-intent strategy across the full search cluster: all airport-specific metadata, application schema, directory messaging, navigation and internal-link anchors now lead with “when should I leave” rather than the calculator format. Added a source-backed planning caveat and review date, removed a repetitive conversion section, and instrumented personalized answer generation, timing-option interest and breakdown engagement with intent/location context through the calendar-to-alarm funnel.
- Verification: airport answer-intent, leave-time, departure-status and calendar-link regression tests passed; the optimized production build passed its type and lint checks; the generic calculator, generated answer, Newark page and airport directory were reviewed at 320, 375, 414, 768 and 1280 px with no horizontal overflow or browser console errors. Production deployment `dpl_CGCKtz1sscjpnmcdypQ69ZBmfAEP` reached Ready, the primary alias served the answer-led titles, canonicals and trust copy, the Vercel error scan was clean, and the live audit passed all 165 URLs with zero issues.

- Reframed the airport leave-time experience around the traveler’s actual question—what time they need to leave to make their flight—while keeping the calculator as the first task. The completed flow now moves from a circumstance-specific result to saving that leave time on the calendar and only then to OnTimer’s persistent-alarm handoff. Removed a duplicated generic keyword section and documented the calculator-first SEO/GEO rule.
- Verification: leave-time, departure-status and calendar-link regression tests passed; the optimized production build passed; the question-to-calculator first screen and result-to-calendar-to-alarm sequence were reviewed at 320, 375, 414, 768 and 1280 px with no horizontal overflow.

- Added concise “Free. No account required.” reassurance inside the Medication Schedule Generator card without changing its hero or workflow. Standardized the existing green eyebrows on the Airport Leave Time, Departure Time, and Wake-Up Time calculators as “Free [calculator description] · No sign-up required,” preserving their H1s, metadata, structured data, layouts, and functionality.
- Verification: optimized production build passed; all four affected pages were checked at 320, 375, 414, 768, and 1280 px with no clipping, horizontal overflow, excessive eyebrow height, or console errors.
- Standardized production releases behind `npm run deploy:prod` using a repository-pinned Vercel CLI and the existing linked project, eliminating repeated `npx @latest` downloads, global npm-cache failures and inconsistent deployment commands. Updated the permanent README and repository guidance.
- Verification: the pinned local CLI reports version 58.11.0; the optimized production build passed before the workflow change.
- Refocused the Medication Schedule Generator hero on the user outcome (“Never miss another dose”) while preserving medication-schedule and calendar-event relevance in the supporting copy. Added a concise crawlable explanation of the free generator to the existing calendar-system section; metadata, canonical, breadcrumb, and structured data remain unchanged.
- Corrected the airport calculator result status so a recommended leave time in the past explicitly says how long ago the user should have left, near-term departures show urgency, and future results show a live countdown instead of a misleading buffer-only “plenty of time” message. Changed the default flight departure from two hours to four hours ahead so the initial domestic planning scenario usually remains actionable after drive, parking, security and terminal time are included.
- Verification: added regression coverage for past, urgent, comfortable and risky departure states; leave-time tests and the optimized production build passed.
- Reduced paid address-autocomplete traffic without changing the four-character threshold or removing Google-quality results: lookups now wait 900 ms, run only while a field is focused and actively edited, and give pasted complete addresses a 1.4-second pause.
- Reused the local destination catalog for Airport Theory and cruise-terminal searches before falling back to Google, and tightened autocomplete abuse protection to 60 requests per IP per hour while preserving unrestricted manual address entry.
- Verification: autocomplete and API cost-guard regression tests passed; optimized production build passed; production returned local BWI and PortMiami suggestions while rejecting a direct untrusted autocomplete request with HTTP 403.

### 2026-08-12

- Aligned the patient-facing private medication-link result with the established medication scheduler workbench, including the same review hierarchy, “Your next step” calendar action, responsive post-export ordering, OnTimer alarm handoff, and calendar-import help. Removed the OnTimer promotion from the provider workbench and moved it into a short provider education section beneath the tool.
- Verification: medication and private-link regression tests and the optimized production build passed.
- Restored one clear patient calendar action that downloads the complete medication schedule—including every recurring dose time—in a single cross-calendar file, removing the confusing per-dose Google Calendar buttons without adding OAuth.
- Added a desktop-only post-download help flow, later replaced on 2026-08-15 by the less interruptive inline confirmation and disclosure pattern.
- Verification: calendar-link and medication-schedule regression tests and the optimized production build passed.

### 2026-08-11

- Replaced the provider medication attachment handoff with a private schedule-link flow: schedule data is encoded only in a URL fragment, never sent in the page request, and is removed from the address bar after the patient page validates it. Patients can review dose details and add the resulting recurring `.ics` schedule to their calendar without an account. Analytics is disabled on the patient import route so schedule fragments and imported values cannot be captured.
- Verification: added round-trip and invalid-payload link tests; medication regression tests and the optimized production build passed.
- Added a geo-targeted cookie consent gate for Google Analytics: Edge Middleware (`src/middleware.ts`) classifies visitors as regulated (EU/EEA, UK, Switzerland) or not via `x-vercel-ip-country`, and `GoogleAnalytics.tsx` only loads GA for regulated visitors after they accept a new banner (`CookieConsentBanner.tsx`). Non-regulated visitors (including the US) see no banner and GA continues to load immediately, unchanged from prior behavior.
- Verification: production build passed; middleware cookie classification confirmed via direct requests with `DE`, `US`, and no `x-vercel-ip-country` header (including confirming a client-supplied `x-vercel-ip-country` header is stripped by Vercel's edge and cannot be spoofed against production); consent-gating logic (`src/lib/consent.ts`) unit-verified for regulated+no-consent (blocked), regulated+granted (allowed), and non-regulated (allowed by default) states; homepage checked in-browser with no banner shown for the default non-regulated case.
- Discovered `/privacy` and `/terms` redirect to static files (`public/OnTimer_Privacy_Policy.html`, `public/OnTimer_Terms_of_Service.html`) rather than the Next.js pages at those routes, so those static files — not `src/app/privacy/page.tsx` — are the policy actually served to visitors and referenced by the Footer. Added a named Google Analytics disclosure and a description of the new UK/EU/EEA/Switzerland cookie-consent gate to the live `OnTimer_Privacy_Policy.html` (Sections 2.5 and 5.7) and bumped its Last Updated date. Pointed the new consent banner's Privacy Policy link directly at the canonical static URL to avoid the redirect hop. The corresponding `src/app/privacy/page.tsx` / `src/app/terms/page.tsx` Next.js pages remain unreachable, stale duplicates by explicit choice — left alone for now.

### 2026-08-10

- Added a privacy-first provider medication calendar handoff tool that reuses the existing medication scheduler's frequency, time-slot, duration, validation, and calendar-file patterns, plus provider instructions and an explicit download-then-email workflow. The form excludes patient identifiers and analytics field values.
- Verification: provider recurrence and escaping regression coverage, the medication test suite, and the optimized production build passed; the calendar-download CTA was shortened for narrow layouts with its filename retained as supporting text.
- Promoted OnTimer to the first mobile action after a medication calendar export, moving the compact import confirmation and troubleshooting beneath the App Store offer while preserving the desktop action-rail sequence where both steps remain visible together.
- Verification: after export, the OnTimer offer rendered before the calendar confirmation at 375 px and the original confirmation-first sequence remained at 1280 px; neither layout produced horizontal overflow. Medication regression tests and the optimized production build passed.
- Connected overnight-dose guidance to the dose time that triggered it: affected time fields and their readable labels now share the warning panel's amber treatment and reference the guidance for assistive technology without incorrectly marking an intentional overnight time as invalid.
- Verification: a three-dose schedule highlighted only its midnight field at 375 px, exposed the warning relationship to assistive technology, and produced no horizontal overflow; medication schedule and calendar-export tests and the optimized production build passed.
- Corrected the medication scheduler's custom-duration field so mobile users can clear and replace the default value without an injected `1` corrupting the next entry; the field now accepts a temporary empty editing state, opens a numeric keyboard, and normalizes to the supported 1–365 day range only after editing. Simplified the OnTimer benefit copy to emphasize automatic schedule-to-alarm conversion.
- Verification: on a 375 px mobile viewport, the 60-day default was replaced directly with 5 and generated a five-day schedule without overflow; medication schedule and calendar-export tests and the optimized production build passed.
- Reworked the medication scheduler result into a review-and-action workbench: completed setup fields now collapse after generation, dose times and safety guidance remain visible, and calendar export plus OnTimer are presented together as the next two actions. After export, technical calendar-import help moves into a collapsed disclosure beneath the OnTimer offer.
- Verification: medication schedule and calendar-export regression tests and the optimized production build passed; generated and post-export states were checked at 320, 375, 414, 768, and 1280 px with no horizontal overflow or console errors.

### 2026-08-09

- Moved alternate-calendar import help beneath the post-download OnTimer offer, replacing the oversized `.ics` instruction card with a compact, truthful download confirmation and an expandable help section so the App Store call to action remains immediately visible.
- Simplified the calendar next-step panel across Airport, Cruise, General Leave Time, and Wake-Up, and replaced generic post-calendar conversion copy with outcome-specific promises for catching a flight, boarding a cruise, reaching a destination, or leaving for an appointment.
- Replaced the airport calculator's generic post-calendar OnTimer pitch with flight-specific action copy and simplified the alternate-calendar link so the result funnel stays focused on catching the flight.
- Made calendar destinations explicit across the timing calculators: Google Calendar is now labeled as such, while Apple Calendar, Outlook, and other calendar users receive a standards-based `.ics` option with provider-accurate handoff instructions.
- Verification: calendar-link and leave-time regression tests and the optimized production build passed; the Google and `.ics` choices, alternate-calendar state transition, generated download metadata, console, and 320 px result layout were checked interactively.
- Standardized the Airport, Cruise, General Leave Time, and Wake-Up result journeys around a progressive calendar handoff: save the useful event first, then elevate OnTimer as the automatic persistent-alarm layer. Wake-Up saves the underlying arrival appointment rather than misrepresenting a calendar event as a wake-up alarm, while Airport Theory continues to route risky results through the safe calculator first.
- Added shared Google Calendar event construction, accurate “calendar opened” states, retry actions, and funnel analytics for calendar handoffs and post-calendar App Store prompts. Medication tooling was intentionally left unchanged.
- Verification: calendar-link, autocomplete, and leave-time regression tests passed; the optimized production build passed; calendar payloads and pre/post-handoff states were exercised for General Leave Time, Wake-Up, and Cruise; Airport, Cruise, General, Wake-Up, and Airport Theory showed no horizontal overflow at 320, 375, 414, or 768 px and emitted no console errors.
- Refocused the generic airport calculator result flow around one clear next step: add the recommended leave time to Google Calendar, then promote OnTimer as the automatic persistent-alarm layer after the calendar handoff opens.
- Added local airport-catalog autocomplete for short IATA-code input such as BWI while preserving the four-character minimum for paid Google Places requests.
- Verification: autocomplete regression tests and optimized production build passed; the BWI match, pre/post-calendar conversion states, console, and horizontal overflow were checked at 320, 375, 414, and 768 px.

### 2026-08-07

- Documented the medication generator's permanent UX, calendar-import, next-occurrence, timezone, alert, conversion, and regression-testing contracts in `CLAUDE.md` and `README.md`.
- Restored “Add to Calendar” as the medication generator's primary export CTA while retaining the concise iPhone confirmation instruction beneath it.
- Replaced the medication generator's OnTimer handoff with the concise dose-specific message “Don't miss your doses. Get OnTimer free and turn this schedule into alarms.”
- Corrected the iPhone calendar-import handoff so the site no longer implies that opening an ICS preview saved the schedule: the primary action now says “Continue to Calendar,” explicitly instructs users to tap Apple’s “Add To Calendar” confirmation, and reports only that the import screen opened. Calendar events now include an at-time alert instead of the “Alert: None” shown in Apple’s preview. Replaced the exhaustive timezone picker with Local (when needed), ET, CT, MT, PT, AZ, AK, HI, and UTC. Verified against the supplied iPhone screenshots, medication export tests, production build, and browser interaction.
- Calendar exports now start each recurring dose series at its next future occurrence, so a same-day dose time that has already passed begins tomorrow while later doses still begin today. Overnight dose offsets are preserved without creating extra calendar series. Tightened the OnTimer handoff around persistent alarms and active dismissal. Verified with deterministic timezone-aware calendar export tests.
- Simplified the medication generator's conversion state: removed the long explanatory pitch, introduced a compact automatic-alarm benefit, and made OnTimer the primary next step after calendar export while retaining a quiet download-again action. Verified with medication schedule tests, a production build, and responsive browser checks.
- Reframed the medication page around its free calendar-schedule generator, replacing the long article-led hero and "Direct Answer" box with a concise outcome-led headline and an above-the-fold workbench layout.
- Kept the supporting SEO/GEO article, FAQ schema, safety language, and related guides below the tool while updating page metadata to describe the generator directly.
- Prevented desktop reloads from restoring a stale mid-article scroll position while preserving ordinary history navigation behavior.
- Verification: medication schedule tests, optimized production build, reload behavior, and responsive review at 320, 375, 414, 768, and 1280 px.

### 2026-08-06

- Added an explicit time-zone selection and an overnight-dose notice to the medication schedule generator so calendar exports do not silently create surprising late-night reminders.
- Kept dose times editable, clarified midnight labeling, and preserved evenly spaced defaults without interpreting or changing prescribed timing.
- Moved the detected time zone beside the first-dose time, shortened frequency controls, added four-times-daily and custom-time schedules, and reduced the overnight warning to a compact notice before the calendar action.
- Compressed first-dose time and its detected zone into one inline control, using short labels such as ET and PT, and removed repeated timezone guidance from the form and results.
- Verification: medication-schedule regression tests and the optimized production build passed.

### 2026-08-03

- Added canonical calculator-start, calculator-completion, automatic-alert CTA impression/click, and App Store outbound-click events to the general and airport leave-time funnels.
- Added durable first-party attribution context (landing page, source, available search term, calculator type, CTA variant, and anonymous attribution token) to GA4 conversion events without mislabeling outbound clicks as installs.
- Verification: TypeScript checks, leave-time tests, and the optimized production build passed.

### 2026-07-31

- Added the remaining 24 airports in the planned 50-airport international collection, with locally specific terminal, road, rail, bus, transfer and inside-airport guidance for each destination.
- Added a catalog regression guard requiring four substantial airport-specific planning facts per international page and rejecting exact reuse of those contextual facts across airports.
- Verification: leave-time tests and the optimized production build passed; all 24 new routes were statically generated; the destination-texture guard passed; Barcelona, Lima, Bangkok and Nairobi were reviewed locally with no console errors or horizontal overflow at 320, 375, 414 and 768 px.

### 2026-07-28

- Added 25 indexable international airport time-to-leave calculators across Europe, the Middle East, Asia-Pacific, India, Canada and the Philippines, each with airport-specific terminal, road, rail and final-transfer guidance.
- Generalized the international security-planning path so non-U.S. airports use conservative airport-security estimates and never expose TSA PreCheck/CLEAR controls or imply live TSA data.
- Added permanent release checks for source depth, international security language and representative responsive review of large destination catalogs.
- Verification: leave-time tests and the optimized production build passed; all 25 routes were statically generated; Changi, Mumbai, Toronto Pearson and Melbourne were reviewed locally with no console errors or horizontal overflow at 320, 375, 414 and 768 px.
- Rewrote the Heathrow airport guide and shared airport-page guidance in direct traveler language, replacing publisher-facing and template-like phrasing with clear actions around terminals, routes, transfers, check-in deadlines and day-of-travel checks.
- Added permanent human-readability checks for programmatic destination pages to the SEO release checklist.
- Verification: production build and leave-time tests passed; Heathrow copy was reviewed from hero through FAQ for consumer clarity.
- Hardened the Google Places and Routes proxy endpoints against unauthenticated direct use and request bursts with same-origin enforcement, input bounds, per-IP limits, and global per-instance safety limits.
- Reduced autocomplete request volume by requiring four characters, increasing debounce time to 600 ms, and cancelling stale browser requests.
- Removed paid travel-time retry fan-out so one calculator submission can make at most one upstream Routes API request; failures now use the existing manual-entry fallback.
- Bounded the warm-instance travel cache and corrected documentation that had incorrectly described it as cross-instance caching.
- Added automated API cost-guard tests and a permanent paid-API cost-safety checklist.
- Verification: API cost-guard, autocomplete, and leave-time tests passed; the optimized production build passed; deployed direct calls return 403 and invalid same-origin requests stop before paid upstream calls.
- Confirmed from Google Cloud Billing that the incident was concentrated in the `ontimer-timetofly-calculator` project: the Places API SKU `Autocomplete without Places Details - Per Session` recorded 9,173 requests and $8.77 in charges. The July 1–28 billing overview showed $13.57 gross cost, $4.54 savings, and $9.03 net cost.
- Verification: reviewed billing by project, service, and SKU. Places API (New) still exposed autocomplete limits of 1,500 requests/day and 600 requests/minute; provider-side quota reduction remains required.

## 2026-07-27

- Corrected cruise-terminal URLs that had been emitted under the airport route. Misclassified legacy URLs now permanently redirect to their canonical cruise pages.
- Restricted the airport calculator directory to airport destinations, preventing future airport/cruise route mismatches.
- Added missing canonical tags across indexable marketing pages, the blog, blog posts, and static legal pages.
- Added 11 eligible supporting content pages to the sitemap.
- Replaced internal links that unnecessarily passed through redirects with direct canonical links.
- Shortened privacy and terms redirects by sending them directly to the canonical `www` hostname.
- Added a repeatable production SEO audit and release checklist.
- Verified production with a complete crawl: 114 sitemap URLs, 115 discovered internal URLs, zero broken links, zero avoidable internal redirects, zero canonical issues, zero erroneous `noindex` pages, and zero destination-route mismatches.
